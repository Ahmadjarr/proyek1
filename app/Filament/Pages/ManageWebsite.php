<?php

// ManageWebsite page removed — feature disabled to prevent access at /admin/manage-website.
// The file is intentionally left as a harmless stub so existing configuration and references remain intact.

return;